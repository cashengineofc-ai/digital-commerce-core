# Cash Engine PRO — estado verificado em 18/09/2026

## Banco aplicado

Projeto: `wxmvlrxbbdtqcjsfpwxt` (Cash Engine Design / Cash Engine PRO).

Recibos confirmados em `supabase_migrations.schema_migrations`:

- `20260918070000` — Equipe, permissões e autorização por empresa.
- `20260918071000` — Conta, empresa, segurança e integrações.
- `20260918075000` — pacote de comércio, financeiro, afiliados, relatórios, admin, notificações, API e suporte.

O último pacote foi aplicado em uma transação única, com uma condição que exigiu ausência de pedidos, transações, saques e lançamentos anteriores. A chamada retornou cancelamento de transporte; consultas posteriores confirmaram o recibo de commit e a instalação. Não foi reenviada a transação.

O manifesto `20260918_manifest.json` registra a ordem e SHA-256 dos arquivos exatos. As versões antigas têm números repetidos e parte do banco foi criada manualmente antes desta sessão: **não executar `supabase db push` cegamente nem reaplicar toda a pasta**. O recibo `75000` representa o pacote listado no manifesto, não um único arquivo antigo. Uma instalação limpa ainda precisa de uma normalização separada da cadeia histórica.

## Correções desta sessão

- Restaurado o reconhecimento do proprietário e unificados os contextos de empresa usados pelas RPCs e pelo RLS.
- Usuários bloqueados e membros removidos deixam de receber autorização; ser dono de outra empresa não concede poderes nesta empresa.
- Bloqueadas mutações diretas de cargos, convites e registros financeiros pelo navegador; gestão passa pelas RPCs.
- Convites não concedem função administrativa ou permissões superiores às do solicitante. Tokens inválidos/nulos e aceite repetido são rejeitados.
- Corrigida rotação de credenciais no Vault com serialização por empresa/provedor e nomes exclusivos.
- Corrigidos contratos legados de funções, coluna de versão do pedido, nomes de colunas de chargeback/busca e resolução de pgcrypto.
- Corrigidas ambiguidades SQL no retry de pedido, busca global e extrato, além do tipo retornado pela listagem de saques.
- Atualizada a árvore de rotas gerada, que ainda não incluía várias rotas existentes.

## Evidências de validação

- Build local: passou (`npm run build`).
- TypeScript local: passou (`npx tsc --noEmit`).
- `npm ci` encontrou inconsistência prévia no package-lock; o build usou instalação sem alterar o lock. O CI existente usa Bun. Não houve novo CI remoto, pois o push foi bloqueado.
- 154 nomes de RPCs referenciados estaticamente no código encontrados no banco. Isso confirma presença, não teste individual de cada função.
- `team_settings_rollback.sql`: passou após aplicação; cobre isolamento, promoção indevida, convite/aceite único, troca de contexto, remoção, leituras de configurações e rotação/desconexão de credencial sintética.
- `commerce_runtime_rollback.sql`: passou após aplicação; produto → oferta → checkout publicado → bump publicado → link único → pedido → retry idempotente → confirmação apenas no banco → split → lançamento único → relatórios/busca/extrato/saldo.
- `post_migration_diagnostic.sql`: passou após aplicação.
- Testes sintéticos foram executados dentro de transações terminadas com ROLLBACK. Nenhum provedor foi chamado e nenhum dinheiro real foi movimentado.

## Pendências reais

- **Envio dos commits:** a revisão automática rejeitou o push para `main`, pois a alteração sincroniza o projeto Lovable. Os commits estão locais; não houve tentativa por rota alternativa. Precisa da autorização explícita do usuário para esse envio.
- **Administrador global:** a consulta encontrou zero perfis ativos com essa autorização. Não foi promovida uma conta arbitrária; é necessário identificar a conta correta para o bootstrap confiável.
- **Serviços externos:** não foi possível verificar pela rede as oito Edge Functions; não afirmar que estejam publicadas. Deploy de código das funções, secrets, configuração de provedor, VAPID/e-mail e agenda dos workers ainda precisam de confirmação operacional.
- **Teste pelo navegador e pagamento externo:** não realizados. Os testes SQL não comprovam entrega push/e-mail, webhook externo, autenticação visual ou liquidação bancária.
- **Cobertura financeira restante:** concorrência entre conexões, afiliado remunerado, reembolso parcial/total e saque conciliado ainda precisam de testes dedicados. Nenhuma transferência real deve ser executada apenas para teste.

Para retomar: obter autorização para enviar os commits locais, revisar a configuração/deploy das funções e identificar o primeiro Admin Global; depois validar os fluxos restantes em ambiente apropriado.
